{
    "name": "color-tabs-vscode-ext",
    "displayName": "color-tabs-vscode-ext",
    "description": "changes the active tab color based on regex",
    "version": "0.1.0",
    "publisher": "orepor",
    "repository": {"url": "https://github.com/oreporan/color-tabs-vscode"},
    "engines": {
        "vscode": "^1.30.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "*"
    ],
    "main": "./out/extension",
    "contributes": {
        "configuration": {
            "type": "object",
            "title": "ColorTabs",
            "properties": {
                "colorTabs.config": {
                    "type": "array",
                    "default": [{"regex": ".*\/web\/.*", "color": "#c0d8e3"},{"regex": ".*\/mobile\/.*", "color":  "#e18a7a"}],
                    "description": "list of mappings from path to color"
                }
            }
        }
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "npm run compile && node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.6.1",
        "vscode": "^1.1.6",
        "tslint": "^5.8.0",
        "@types/node": "^7.0.43",
        "@types/mocha": "^2.2.42"
    }
}
